# Vite Solid Panda
Quick start template for a new project using Vite, SolidJS & PandaCSS.

## Usage

```bash
$ npm install # or pnpm install or yarn install
```

```bash
$ npm run dev # or pnpm dev or yarn dev
```